# AIS-worker

## Prerequisites

- `npm i`
- Copy `.env.example` to `.env` and fill out appropiate values
- `git submodule update --init --recursive`

## Development

- `npm run dev`

## Running Tests

`npm test`
`npm test --cov`
